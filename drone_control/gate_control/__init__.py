"""Recurrent vision gate-policy deployment — flies the Air75 through a gate from a
betaflight-gym jax.export policy. Uses Vicon for proprio + the safety kill, but the
policy itself never sees position. See gate_flight.py."""
